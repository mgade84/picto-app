import React from "react";
import { StyledFirebaseAuth } from "react-firebaseui";
import "firebase/auth";

export default function LoginPage() {
    const uiConfig = {
        firebase,
    };

    return <StyledFirebaseAuth></StyledFirebaseAuth>;
}
